'use strict';

module.exports = async name => {
 
  // Configs
  const { postgresConfig } = require('../secret/secret');
  const { host, port } = require('../secret/secret').serverConfig;
  ////////////////////////////////////////////////////////////////////

  


  
return result;
  
};
